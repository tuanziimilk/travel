# About 质检对比工具（运营 SOP）

本文档用于指导运营同学日常使用 `About 质检对比工具` 完成单条评分、批量评分、历史复盘与看板分析。

---

## 1. 工具目标

- 统一评估 `About` 文案质量（线上 / AI 优化 / OP 复核）。
- 快速识别可发布版本、问题点与优化方向。
- 批量处理任务并沉淀历史数据，支持后续复盘与对比。

---

## 2. 页面功能总览

- `手动评分`：单条输入并即时评分，适合精修与对比。
- `批量上传`：上传 CSV/XLSX 异步评分，适合大批量任务。
- `历史批次`：查看已入库批次、备注、得分表现和详情。
- `评估看板`：按筛选条件看聚合指标（均分、最佳版本占比、分布）。

---

## 3. 手动评分 SOP

### 3.1 适用场景

- 新文案上线前质检。
- 线上 vs AI 优化效果对比。
- 关键站点（重点国家）逐条审核。

### 3.2 操作步骤

1. 打开 `手动评分` 页面。
2. 填写基础信息：
   - `TermID`（建议填）
   - `TermName`（选填）
   - `Domain`（建议填）
   - `Country`（必填，使用下拉选择）
   - `Uploader`（必填，下拉选择）
   - `备注`（选填，建议填测试目的）
3. 填入文本：
   - `About-线上`（必填）
   - `About-AI优化`（必填）
   - `About-OP复核`（选填）
4. 点击 `评分对比`。

### 3.3 结果解读

- **分项结果**：每个版本都有总分、A/B/C/D 分项、是否可发布。
- **优势/劣势/建议**：按条列展示，直接用于改写。
- **对比可视化**：
  - 版本排名
  - 最佳版本
  - 关键差异（可用于复盘汇报）
- **执行信息**：耗时、Token、费用估算（USD/CNY）、模型名称。

### 3.4 PIN 测试案例说明

- 点击 `PIN 测试案例` 可快速填充标准测试案例并显示结果。
- 再次点击可取消显示。
- 用于演示和联调，避免重复手填。

---

## 4. 批量上传 SOP

### 4.1 上传前准备

- 文件格式：`.csv` 或 `.xlsx`
- 表头必须严格一致：

`TermID, TermName, Domain, Country, About-线上, About-AI优化, About-OP复核`

- 可点击页面 `下载上传模板` 获取标准示例。

### 4.2 操作步骤

1. 打开 `批量上传` 页面。
2. 选择 `上传人`（必选）。
3. 填写 `批次备注`（建议填写，如“UK-2月回归”）。
4. 选择上传文件。
5. 点击 `开始上传并评分`。

### 4.3 进度与结果

- 任务为异步队列执行。
- 页面可查看：
  - 任务状态
  - 已处理数 / 总数
  - 失败数
  - 进度条
- 点击 `刷新进度` 可手动拉取最新状态。
- 任务完成后可下载 `本批 CSV` 结果。

---

## 5. 历史批次 SOP

### 5.1 筛选维度

- 上传人
- 国家
- 开始日期 / 结束日期

### 5.2 列表字段含义

- 创建时间：批次入库时间
- 上传人：责任人
- 备注：本批次业务说明
- 行数：该批次记录数
- 均分（线上/AI/OP）：三版本平均分
- 优胜版本：该批次总体更优版本

### 5.3 详情查看

- 点击 `查看详情` 进入行级结果（支持分页）。
- 可用于核查某批异常分数、失败行等。

---

## 6. 评估看板 SOP

### 6.1 使用方式

1. 选择筛选条件（上传人、国家、日期范围）。
2. 查看聚合图表：
   - 版本均分对比
   - 最佳版本占比
   - 分数分布（分桶）

### 6.2 推荐用途

- 周会/月会汇报 AI 优化效果。
- 比较不同国家/上传人的质量表现。
- 追踪优化策略上线前后变化。

---

## 7. 运营最佳实践

- `Country` 必须准确选择，避免本地化评分偏差。
- 单条测试先跑 `手动评分`，确认质量后再批量。
- 批量任务必须填 `备注`，便于历史追溯。
- 对“可发布”以外结果，优先按建议逐条修正再复测。
- 大批量时先抽样 10 条手动验证，再全量跑。

---

## 8. 常见问题（FAQ）

### Q1：为什么上传后没立刻出结果？

- 批量是异步队列执行。请在任务进度区点击 `刷新进度`。

### Q2：为什么有失败行？

- 常见原因：原文为空、格式问题、模型调用异常。可在结果中查看失败统计并重试。

### Q3：为什么历史里看不到某次手动评分？

- 请确认手动评分时是正常提交成功，并填写了上传人。

### Q4：为什么评分和人工主观感受不一致？

- 评分基于统一规则与模型输出。建议结合“优势/劣势/建议”做二次判断。

---

## 9. 运营交付清单（建议）

每次批量任务后，建议沉淀以下内容：

- 批次备注（任务目的）
- 批次总行数、失败数
- 线上/AI/OP 均分
- AI 相对线上提升
- 典型高分与低分案例各 2-3 条

---

## 10. 版本与维护

- 文档版本：`v1.0`
- 适用范围：当前 About 质检对比工具前端页面（手动/批量/历史/看板）
- 如页面字段或流程有调整，请同步更新本 SOP。

